diff --git a/src/controls/Units.qml b/src/controls/Units.qml
index 75ff962..2037a54 100644
--- a/src/controls/Units.qml
+++ b/src/controls/Units.qml
@@ -20,6 +20,7 @@
 import QtQuick 2.4
 import QtQuick.Window 2.2
 import org.kde.kirigami 2.4
+import Ubuntu.Components 1.2 as UbuntuComponents
 
 pragma Singleton
 
@@ -31,11 +32,10 @@ QtObject {
     id: units
 
     /**
-     * The fundamental unit of space that should be used for sizes, expressed in pixels.
-     * Given the screen has an accurate DPI settings, it corresponds to a width of
-     * the capital letter M
+     * on Ubuntu Touch, gridUnit works like an alias to the
+     * ubuntu-ui-toolkit one
      */
-    property int gridUnit: fontMetrics.height
+    property int gridUnit: UbuntuComponents.units.gu(1)
 
     /**
      * units.iconSizes provides access to platform-dependent icon sizing
